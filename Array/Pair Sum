#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a[100];
	int n;
	cin>>n;
	for(int i=0;i<n;i++)
	{
		cin>>a[i];
	}
	int key=0;
	cin>>key;
	for(int i=0;i<n-1;i++)
	{
		for(int j=i;j<n;j++)
		{
			if(a[i]+a[j]==key)
			{
				cout<<a[i]<<" "<<a[j]<<endl;
			}
		}
	}
}
